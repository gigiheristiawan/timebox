TimeBox is a macOS menu bar utility for task rotation timeboxing:

> The timer controls **how long you work on a task**, not whether the task is completed.

0.6.0 makes the checkpoint easier to answer and stops Pomodoro mode asking the same question twice. It also includes the battery fix prepared as 0.5.3, which was never published on its own.

## The checkpoint shows what comes next

When a block runs out, the checkpoint asks whether to complete the task, set it aside or take a break — and moving on starts the next task in the queue. It used to do that without saying which task that was. It now shows an **Up next** line above the choices, naming the task that will start, or *Nothing else queued*.

The name is the task the app will actually start, not simply the next row in the queue: a daily already done today is stepped over here exactly as it is when the next task begins.

## Pomodoro mode stops asking twice

With Pomodoro mode on, a task's checkpoint can arrive partway through a pomodoro. The checkpoint already offers a break. Answering without one used to leave the Pomodoro clock running, so a checkpoint reached 20 minutes into a pomodoro was followed five minutes later by the Pomodoro prompt, asking the question just answered.

Completing the task, setting it aside or extending it at a checkpoint — with the break left unchecked — now starts a fresh 25 minutes, just as *Skip & continue* does on the Pomodoro prompt. Taking the break works as before: the clock restarts when the break ends.

Completing a task early from the popover or the main window does not reset the clock. No break was offered there, so none was turned down.

## Much less battery while a block runs

Every second a block was running, TimeBox rewrote its entire database to save a state that had not changed since the second before, sent a full copy of its state to windows that were hidden, and redrew the countdown four times a second for a number that changes once. It now saves only when something has actually changed, updates only the windows on screen (each catches up the moment it opens), and redraws the countdown once a second.

Durability is unaffected: every real transition is still saved before the app acts on it, so quitting mid-block, a crash or a Mac going to sleep all resolve exactly as before.

## Everything else is unchanged

No migration, no schema change and no new setting. Your existing data is untouched.

## Privacy

No account, no network calls, no telemetry. Everything is a local SQLite database under `~/Library/Application Support/xyz.gigiheristiawan.timebox/`. This release adds no entitlement and no new source of data.

## Upgrading

Your existing database opens unchanged. There is no migration in this release and nothing to do by hand.

There was no 0.5.3 release: its changes are the battery fix above.

If you are coming from 0.4.1 or earlier, read the [0.5.0 notes](0.5.0.md) as well — it carries migration 007 and it is where the timer overlay and the tinted current block arrived.

If you are coming from 0.3.2 or earlier, read the [0.4.0 notes](0.4.0.md) too — two migrations, and where daily tasks, Pomodoro mode and the report arrived.

If you are coming from 0.2.0 or earlier, read the [0.3.0 notes](0.3.0.md) as well — in particular the one manual step there: every task added before 0.3.0 is stored as **Medium** priority regardless of what was picked, and can only be corrected with the editor.

## Install

Requires **macOS 13 or later**. Universal binary — Apple Silicon and Intel.

Open the DMG and drag TimeBox to Applications. The build is signed with a Developer ID certificate and notarized by Apple, so it opens without a Gatekeeper warning.

## Known limitations

- macOS only.
- **The overlay's opacity fades the whole card, text included** — at 0% it is invisible but still there. If it seems to have vanished, check the slider before the toggle.
- A native full-screen app can cover the overlay. That is macOS's call, not ours.
- **The checkpoint always opens on the main display**, which is not configurable. If you want it on the other screen, change which display is the main one in System Settings → Displays.
- **Sleep is not yet detected.** Closing the lid mid-block is still recorded as work, capped at the block's allocation — and counts toward the Pomodoro clock as well. Quitting is handled; sleeping is not.
- Pomodoro's twenty-five minutes is not configurable, the break offered is one length, and *Skip & continue* means the break is never truly forced.
- Daily is the only recurrence, and there are no streaks.
- The report has no export, no charts, and no range other than a week.
- Worked time recorded before 0.3.2 keeps its old per-block attribution: a block from an earlier version that crossed midnight still reports all of it on the day it started.
- A task's block duration can only be increased, never reduced. If you want a shorter block, remove the task and add it again.
- No import, export, or sync — the database is local and yours to back up.
